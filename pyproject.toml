[tool.poetry]
name = "remarkable-wifi-conf"
version = "0.1.0"
description = ""
authors = ["Pascal Widdershoven"]

[tool.poetry.dependencies]
python = "^3.8"
PyQt5 = "^5.15.2"

[tool.poetry.dev-dependencies]

[build-system]
requires = ["poetry>=0.12"]
build-backend = "poetry.masonry.api"
